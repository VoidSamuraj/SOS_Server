ktor:
    application:
        modules:
            - ApplicationKt.module
    deployment:
        host: "localhost"
        sslPort: 8443
    security:
        ssl:
            keyStore: "keystore.jks"
            keyAlias: "myKeyAlias"
            keyStorePassword: "qwerty"
            privateKeyPassword: "qwerty"